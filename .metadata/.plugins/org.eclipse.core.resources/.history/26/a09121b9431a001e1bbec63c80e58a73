<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<style>
div{
  border : 1px dotted orange;
  height : auto;
  padding : 10px;
  margin : 10px;
  font-size : 1.5rem;
}
</style>
<script>
/* 
proc1 = function(){}
이런 방식으로도 function 만들 수 있음
*/
 function proc1(){
	 //숫자1 입력
	 su1 = parseInt(prompt("첫번째 숫자를 입력하세요"));
	 
	 //연산자 입력
	 op = prompt("연산자를 입력하세요\n(+,-,*,/,%중 하나)");
	 
	 //숫자 2 입력
	 su2 = parseInt(prompt("두번째 숫자를 입력하세요."));
	 
	 /*
	 switch (op) {
	case "+":
		res = su1 + su2;
		break;
	case "-":
		res = su1 - su2;
		break;
	case "/":
		res = su1/su2;
		break;
	case "*":
		res = su1 * su2;
		break;
	case "%":
		res = su1 % su2;
		break;
	default:
		break;
	}
	 */
	 
	 if(op=="+"){
		 res = su1 + su2;
	 }else if(op =="-"){
		 res = su1 - su2;
	 }else if(op =="/"){
		 res = (su1/su2).toFixed(2);		//toFixed(2) => 소숫점이하 2자리
	 }else if(op =="*"){
		 res = su1 * su2;
	 }else if(op =="%"){
		 res = su1 % su2;
	 }
	//출력내용 구성 - 34 * 56 = ?
	 res = `${su1} ${op} ${su2} = ${res} <br>`;		
	 
	 //출력 - id=result인 요소를 검색
	 document.getElementById('result').innerHTML = res;
 }

proc2 = function(){
	//점수 입력
	score = prompt("점수를 입력하세요.");
	//점수 비교 - if
	
	if(score>100 || score<0){
		alert("값이 범위를 벗어났습니다.")
		return;
	}
	/*
	if(score>=90){		//90이상
		res = "A학점"
	}else if(score>=80){			//80이상 ~ 90미만
		res = "B학점"
	}else if(score>=70){			//70이상 ~ 80미만
		res = "C학점"
	}else if(score>=60){			//60이상 ~ 70미만
		res = "D학점"
	}else{				//60미만
		res = "F학점"
	}
	*/
	
	 switch (parseInt(score/10)) {
		case 10 : case 9:
			res = "A학점";
			break;
		case 8:
			res = "B학점";
			break;
		case 7:
			res = "C학점";
			break;
		case 6:
			res = "D학점";
			break;
		default:
			res = "F학점";
		}
	
	//출력내용 점수 : 99  결과 : A학점
	res = `점수 : ${score}  결과 : ${res}`;
	
	//출력
	document.getElementById('result2').innerHTML = res;
}
</script>
</head>
<body>
<h3>숫자 2개와 연산자 1개를 prompt를 이용하여 입력 받아 연산자에 맞는 계산결과를 출력하는 프로그램을 작성하시오.</h3>
<input type="button" value="확인" onclick="proc1()">
<br><br>
<div id="result"></div>
<br>
<hr color='red'>


<h3>점수를 prompt를 이용하여 입력받아 학점을 출력하시오<br></h3>
<input type="button" value="확인" onclick="proc2()">
<br><br>
<div id="result2"></div>

<br>
<hr color='red'>


<h3>두 사람의 가위, 바위, 보를 입력 받아 승자를 출력하는 프로그램을 작성하시오</h3>
<input type="button" value="확인" onclick="proc3()">
<br><br>
<div id="result3"></div>
</body>
</html>