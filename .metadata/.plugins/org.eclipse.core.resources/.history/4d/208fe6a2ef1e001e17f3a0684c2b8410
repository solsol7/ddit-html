<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<link rel="stylesheet" href="../css/style.css">
<script>
const arr= ["사과","바나나","복숭아","사과","참외","수박","사과"];

proc1 = () => {
	varr = arr.slice(0,4);
	
	str = `원래배열 : ${arr} <br>`;
	str += `arr.slice(0,4) : ${varr} <br>`
	
	str += `slice 후 배열 : ${arr} <br>`;
	
	document.getElementById('result1').innerHTML = str;
}

proc2 = () => {
	
	start = 0;
	end = 0;

	idx = 0;
	str ="";
	
	//사과와 사과 사이의 리스트 추출
	while(true){
		start = arr.indexOf("사과",idx); //0번째
		end = arr.indexOf("사과", start+1);	//3번째
		
		if(end < 0) break;
		
		newarr = arr.slice(start+1, end);
		str += newarr +",";
		
		//alert(newarr);
		
		idx = end;
		
	}
	
	str = str.slice(0, str.length-1);
	
	document.getElementById('result2').innerHTML = str;
	
}

proc3 = () =>{
	str = `원래배열 : ${arr} <br>`
	
// 	arr.splice(2, 1, 100, 200, 300, 400);
// 	str += `arr.splice(2, 1, 100, 200, 300, 400) 수행 후 : ${arr} <br>`
//------------------------------------------------------------------
	
// 	arr.splice(5, 0, "일", "이", "삼");
// 	str += `arr.splice(5, 0, "일", "이", "삼") 수행 후 : ${arr} <br>`
//----------------------------------------------------------------

	arr.splice(4,2);
	str += `arr.splice(4,2) 수행후 : ${arr} <br>`;
	
	document.getElementById('result3').innerHTML = str;
}
proc4 = () => {
	
	str = `배열 길이 : ${arr.length} <br>`
	
	str2 = arr.join(",");
	
	str += `str2(join후) 길이 : ${str2.length} <br>`;
	
	arrstr = "";
	
	for(let i=0; i<arr.length; i++){
		arrstr += `arr[${i}] = ${arr[i]} <br>`;
	}
	
	str += arrstr + "<br>";
	
	arrstr ="";
	
	for(let i=0; i<str2.length; i++){
		arrstr += `str2[${i}] = ${str2[i]} <br>`;
	}
	
	str += arrstr + "<br>";
	document.getElementById('result4').innerHTML = str;
}
proc5 = () => {
	arr2 = [100, 200, 300, 400, 500];
	
	arr.concat(arr2);
	arr2.concat(arr);
	
	document.getElementById('result5').innerHTML = arr + "<br>";
	document.getElementById('result5').innerHTML += arr2;
}
</script>
</head>
<body>

<h3>proc1() : varr = arr.slice(0,4); <br>
slice후의 원래 배열은 바뀌지 않는다 <br>
proc2() : 사과와 사과 사이의 리스트 출력 </h3>
<input type="button" value="slice" onclick="proc1()">
<input type="button" value="slice" onclick="proc2()">
<br>
<div class="ddres" id="result1"></div>
<div class="ddres" id="result2"></div>
<br>
<hr color="red">

<h3>splice(index, n, a, b, c) <br>
index : 시작위치 <br>
n : 삭제 갯수  - 0일때는 삭제되지 않는다 <br>
a, b, c , ... : 추가되는 요소 <br>
splice(index, n, a, b, c)</h3>
<input type="button" value="splice" onclick="proc3()">
<br>
<div class="ddres" id="result3"></div>
<br>
<hr color="red">

<h3>join(구분자) : 배열요소를 하나의 문자열로 변환</h3>
<input type="button" value="join" onclick="proc4()">
<input type="button" value="concat" onclick="proc5()">
<br>
<div class="ddres" id="result4"></div>
<br>
<hr color="red">
</body>
</html>