<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script>
const rect = { };

proc = () => {
	//속성추가
	rect.width = 8;
	rect.height =5;
	rect.name = "네모";
	
	//메소드 추가
	rect.getArea = function(){
		return this.width * this.height
	}
	
	//출력
	rectPrint("result1");
}

rectPrint = function(result){
	str = `이름 : ${rect.name}<br>`;
	str += `가로 : ${rect.width} <br>`;
	str += `세로 : ${rect.height} <br>`;
}

</script>
</head>
<body>
<h3>const rect = { }; 비어있는 객체를 선언 <br>
나중에 동적으로 속성이나 메소드를 추가 할 수 있다
</h3>
<input type="button" value="확인" onclick="proc1()">
<br>
<div id="result1"></div>
</body>
</html>