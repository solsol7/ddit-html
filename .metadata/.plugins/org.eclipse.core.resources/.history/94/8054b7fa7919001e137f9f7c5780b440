<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<script>
function add(a,b){
  let res = a+b;
  return res;
}

function sub(a,b){
  let res = a-b;
  return res;
}

window.onload = function(){  // window가 load되기 전까지는 수행하지 않는다 =>윈도우가 로드된다 : 바디태그 실행해서 화면에 보이는 것
  //id=result인 div요소를 검색
  let vres = document.getElementById('result');
  vres.innerHTML = "<h1>Hello</h1>";
  vres.style.color = 'red';
  
  let a=10;
  let b=20;
  
/*  res = add(a, b);
  str = a+ " + " + b + " = " + res + "<br>";
  
  res = sub(a, b);
  str += a+ " - " + b + " = " + res + "<br>" */
  
  str = a+ " + " + b + " = " + add(a,b) + "<br>";
  
  vres.innerHTML += str;
}
</script>
<style>
#result{
  border : 1px dotted red;
  width : 50%;
  height : 50px;
  margin : 20px;
  padding : 20px;
  overflow : auto;
}
</style>
</head>
<body>
<!-- <input type="button" value="확인"> -->
<div id="result"></div>
</body>
</html>